{
  "name": "shortlink-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DEMO URL shortener — pipeline walkthrough (TS/Bun, SQLite→D1 Workers).",
  "scripts": {
    "test": "bun test",
    "dev": "bun run src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.28.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0"
  }
}
